using If.Lync;
using System;
using Xunit;

namespace OfficeMap.Lync.Tests
{
    public class LyncStatusTests
    {
        private readonly Config config;

        public LyncStatusTests(Config config)
        {
            this.config = config;
        }

        [Fact]
        public void When_get_user_Then_return_their_status()
        {
          
        }
    }
}
